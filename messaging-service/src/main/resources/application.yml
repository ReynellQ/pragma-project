server:
  port: 8002

spring:
  application:
    name: "food-court-service"

eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}


my:
  twilio:
    auth-token: "c15324bc851291a39ae7290cf720189e"
    



appDescription: "Plazoleta API"
appVersion: "1.0.0"
